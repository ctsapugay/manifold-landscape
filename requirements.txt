# Manifold Landscape — computation engine dependencies.
# Local development only (constraint C-LOCAL). Install into a project venv:
#     python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
numpy>=1.26
sympy>=1.12
scipy>=1.11
mpmath>=1.3
pytest>=8.0
